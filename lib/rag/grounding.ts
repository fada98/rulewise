import type { RetrievedChunk } from "./citations";
export const NO_ANSWER="I couldn't find enough information in the selected documents to answer this reliably.";
export function hasSufficientEvidence(chunks:RetrievedChunk[],threshold=.78){return chunks.some(chunk=>chunk.similarity>=threshold&&chunk.content.trim().length>=40)}
export function buildGroundedPrompt(question:string,chunks:RetrievedChunk[]){const context=chunks.map((chunk,index)=>`[Source ${index+1}: ${chunk.documentName}, page ${chunk.pageNumber}]\n${chunk.content}`).join("\n\n");return `You are RuleWise, an evidence-focused document assistant.\n\nRules:\n- Answer only from the supplied context.\n- Never add rules, facts, or interpretations not supported by the context.\n- Keep the answer concise and distinguish uncertainty clearly.\n- Do not invent citations; citation metadata is added by the application.\n- If the context is insufficient, respond exactly: "${NO_ANSWER}"\n\nContext:\n${context}\n\nQuestion: ${question}`}
